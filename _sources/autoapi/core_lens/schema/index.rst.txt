core_lens.schema
================

.. py:module:: core_lens.schema

.. autoapi-nested-parse::

   Schema introspection utilities for core_lens.



Submodules
----------

.. toctree::
   :maxdepth: 1

   /autoapi/core_lens/schema/detection/index
   /autoapi/core_lens/schema/profile/index


Exceptions
----------

.. autoapisummary::

   core_lens.schema.SchemaDetectionError


Classes
-------

.. autoapisummary::

   core_lens.schema.Resolution
   core_lens.schema.SchemaProfile


Functions
---------

.. autoapisummary::

   core_lens.schema.detect


Package Contents
----------------

.. py:class:: Resolution

   Bases: :py:obj:`str`, :py:obj:`enum.Enum`


   Temporal resolution of a dataset.


   .. py:attribute:: STATIC
      :value: 'static'



   .. py:attribute:: ANNUAL
      :value: 'annual'



   .. py:attribute:: FORTNIGHTLY
      :value: 'fortnightly'



.. py:class:: SchemaProfile(/, **data: Any)

   Bases: :py:obj:`pydantic.BaseModel`


   Validated descriptor for the column layout of one entity's Parquet files.

   ``SchemaProfile`` is a Pydantic model, so all fields are validated at
   construction time.  It is built once per entity — either declared
   explicitly in a concrete :class:`~core_lens.base.entity.BaseEntity`
   subclass, or produced lazily by the schema detection layer when a path is
   first accessed.

   The model is intentionally narrow: it captures only what the rest of the
   library needs to adapt to schema changes at runtime (key identities, geometry
   encoding, time column names, optional bbox columns).  Columns not listed in
   one of the ``extra_*`` fields are ignored rather than rejected, so adding
   a new attribute column to a Parquet file is always a non-breaking change.

   .. attribute:: key_cols

      Column name(s) that form the unique identifier for one entity
      instance.  Must be present in the static file and in every temporal
      file that references the entity.

   .. attribute:: geometry_col

      Name of the geometry column in the static GeoParquet file.

   .. attribute:: geometry_type

      Encoding of the geometry column.  ``"wkb"`` for WKB-encoded
      bytes (standard GeoParquet), ``"wkt"`` for WKT strings, ``"latlon"``
      for separate latitude/longitude columns (requires ``bbox_cols`` to be
      set or a specific lat/lon pair to be documented separately).

   .. attribute:: annual_time_col

      Name of the date/year column in the annual Parquet file,
      or ``None`` if the entity has no annual data.

   .. attribute:: fortnightly_time_col

      Name of the date column in the fortnightly Parquet
      file, or ``None`` if the entity has no fortnightly data.

   .. attribute:: bbox_cols

      Four-tuple ``(minx, miny, maxx, maxy)`` of column names
      holding the pre-computed bounding box for each entity instance in the
      static file.  Used to build the in-memory index without a geometry
      decode pass.  ``None`` if the static file does not carry bbox columns
      (the index will be built by decoding the geometry column instead).

   .. attribute:: extra_static_cols

      Additional attribute columns in the static file that
      the library should surface.  Columns absent from this list are still
      readable via :meth:`~core_lens.base.result.Result.df`; this field
      is used for validation and documentation only.

   .. attribute:: extra_annual_cols

      Additional attribute columns in the annual file.

   .. attribute:: extra_fortnightly_cols

      Additional attribute columns in the fortnightly file.


   .. py:attribute:: model_config

      Configuration for the model, should be a dictionary conforming to [`ConfigDict`][pydantic.config.ConfigDict].


   .. py:attribute:: key_cols
      :type:  list[str]


   .. py:attribute:: geometry_col
      :type:  str


   .. py:attribute:: geometry_type
      :type:  Literal['wkb', 'wkt', 'latlon']


   .. py:attribute:: annual_time_col
      :type:  str | None


   .. py:attribute:: fortnightly_time_col
      :type:  str | None


   .. py:attribute:: bbox_cols
      :type:  tuple[str, str, str, str] | None


   .. py:attribute:: extra_static_cols
      :type:  list[str]
      :value: []



   .. py:attribute:: extra_annual_cols
      :type:  list[str]
      :value: []



   .. py:attribute:: extra_fortnightly_cols
      :type:  list[str]
      :value: []



   .. py:method:: _check_key_cols_non_empty() -> SchemaProfile


   .. py:method:: _check_latlon_needs_bbox() -> SchemaProfile


.. py:exception:: SchemaDetectionError

   Bases: :py:obj:`Exception`


   Raised when the detection layer cannot produce a valid SchemaProfile.

   The message identifies which file and which inference step failed so
   entity authors have an actionable starting point.


.. py:function:: detect(static_path: str, key_cols: list[str], geometry_col: str, annual_path: str | None = None, fortnightly_path: str | None = None) -> core_lens.schema.profile.SchemaProfile

   Introspect Parquet file schemas and return a validated SchemaProfile.

   Reads only Parquet footer metadata — no row data is scanned.  Intended
   to be called once per entity at ``AoI.register()`` time and the result
   cached on the entity instance.

   Detection order for each inferred field:

   * **geometry_type** — derived from the dtype of ``geometry_col``:
     ``Binary`` → ``"wkb"``, ``String`` → ``"wkt"``, ``Float``
     with a detected lon companion column → ``"latlon"``.
   * **bbox_cols** — checked against a fixed set of four-column name
     patterns.  ``None`` if no known pattern is present in the static schema.
   * **annual_time_col** / **fortnightly_time_col** — the first column with
     a ``Date``, ``Datetime``, or ``Int`` dtype whose name matches a known
     set of time-column names.  Falls back to the first ``Date``/``Datetime``
     column regardless of name.
   * **extra_*_cols** — every remaining column after key, geometry, bbox,
     and time columns are accounted for.

   :param static_path: Path to the static GeoParquet file.
   :param key_cols: Entity key column name(s), as declared on the entity.
   :param geometry_col: Geometry column name, as declared on the entity.
   :param annual_path: Path to the annual Parquet file, or ``None``.
   :param fortnightly_path: Path to the fortnightly Parquet file, or ``None``.

   :returns: A validated :class:`~core_lens.schema.profile.SchemaProfile`.

   :raises SchemaDetectionError: If a required column is absent, the geometry
       type cannot be inferred, or any other detection step fails.


